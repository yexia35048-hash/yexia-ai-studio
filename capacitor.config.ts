import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.netlify.enchanting_flan_271421.twa',
  appName: 'Sudoku Master',
  webDir: 'dist'
};

export default config;
